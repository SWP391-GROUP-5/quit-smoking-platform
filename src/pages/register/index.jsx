import React from "react";
import AuthenTemplate from "../../components/authen-template";

const RegisterPage = () => {
  return (
    <AuthenTemplate isLogin={false} />
  );
};

export default RegisterPage;