import React from 'react';
import AuthenTemplate from '../../components/authen-template';

const LoginPage = () => {
  return (
    <AuthenTemplate isLogin={true} />
  );
};

export default LoginPage;